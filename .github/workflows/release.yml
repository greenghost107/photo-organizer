name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Python dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install pyinstaller

      - name: Build frontend
        run: |
          cd frontend
          npm install
          npm run build

      - name: Copy frontend dist to backend/static
        shell: pwsh
        run: |
          if (Test-Path backend/static) { Remove-Item -Recurse -Force backend/static }
          Copy-Item -Recurse frontend/dist backend/static

      - name: Build exe with PyInstaller
        run: |
          cd backend
          python -m PyInstaller --onefile --noconsole --name "DupPhotoLocator" --add-data "static;static" --add-data "scanner.py;." --hidden-import pystray._win32 --hidden-import PIL._imagingtk --distpath ../dist main.py

      - name: Generate SHA256 checksum
        shell: pwsh
        run: |
          $hash = Get-FileHash dist/DupPhotoLocator.exe -Algorithm SHA256
          "$($hash.Hash)  DupPhotoLocator.exe" | Out-File -FilePath dist/DupPhotoLocator.exe.sha256 -Encoding utf8

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/DupPhotoLocator.exe
            dist/DupPhotoLocator.exe.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
